<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:orientation="vertical">

    <TextView
        android:id="@+id/xxChooserTitle"
        style="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:text="Choose your target for this exercise"
        android:textColor="@android:color/white"
        android:textSize="18sp" />

    <com.shawnlin.numberpicker.NumberPicker
        android:id="@+id/xxChooserPicker"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:layout_below="@+id/xxChooserTitle"
        android:layout_centerHorizontal="true"
        app:np_selectedTextColor="@android:color/white"
        app:np_textColor="@android:color/white" />

    <Button
        android:id="@+id/xxChooserButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="15dp"
        android:layout_below="@+id/xxChooserPicker"
        android:layout_centerHorizontal="true"
        android:background="@drawable/training_button_background"
        android:text="Choose this as target"
        android:textColor="@drawable/black_button_text_color" />
</RelativeLayout>